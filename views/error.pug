extends base

block content
  section.hero
    span.tag 404
    h1(class="text-[2.2rem] mt-3") Page not found
    p(class="text-[1.15rem] text-muted") Sorry, we couldn't load that URL.
    .btn-row.justify-center
      a.feature.inline-block(href="/") Back to demos
